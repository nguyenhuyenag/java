package com.collection.queue.deque;

import java.util.ArrayDeque;
import java.util.Deque;
import java.util.Queue;

/**
 * - ArrayDeque: Triển khai hàng đợi (queue) và ngăn xếp (stack) sử dụng một mảng động
 *
 * - ArrayDeque tương tự như như LinkedList nhưng dùng mảng để triển khai còn LinkedList
 * thì sử dụng danh sách liên kết
 */
public class ArrayDequeSample {

    public static void main(String[] args) {
        Queue<Integer> deque = new ArrayDeque<>();
    }

}
